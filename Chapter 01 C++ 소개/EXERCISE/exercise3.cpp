#include <iostream> // 입출력에 필요한 클래스와 객체 정의
using namespace std; // 현재 사용하는 이름 공간을 std로 설정

int main() // 함수의 헤더
{
    cout << "Hello World!\n"; //화면에 해당 문자열 출력
    cout << "C++ is fast\n"; // 상동
    return 0;
}
